REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Set this in the Netlify dashboard under "Environment variables"
# It is used by netlify.toml to proxy /api/* to your live backend
# (You don't need to set this in a .env file – it goes in netlify.toml)
